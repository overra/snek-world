{
  "name": "snek-world",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "npm run cleanup && parcel build index.html",
    "cleanup": "rimraf dist"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "app.css": "^0.1.2",
    "color": "^3.1.2"
  }
}