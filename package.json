{
  "name": "snek-world",
  "version": "0.2.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "parcel serve index.html -d public",
    "build": "npm run cleanup && parcel build index.html -d public",
    "cleanup": "rimraf public"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "color": "^3.1.2"
  }
}
